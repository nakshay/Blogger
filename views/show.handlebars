<header class="masthead text-center text-white d-flex">
    <div class="container my-auto">
        <div class="wrapper">
             <h2 class="form-signin-heading">  {{data.title}} - {{data.author}}</h2>
        </div>
     
        <div class="row">
            {{data.content}}
        </div>
       <span>
       <a class="btn btn-success" href="/user/edit/{{data._id}}" role="button">Edit</a>
       </span>
       <span>
    
       <a id="deletebtn" data-id="{{data._id}}" class="btn btn-danger">Delete Blog </a>
       </span>
    </div>
</header>